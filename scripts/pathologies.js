document.addEventListener("DOMContentLoaded", function(event) {
    	var typeSelect = document.getElementsByName("type")[0];
    	if (typeSelect){
			//typeSelect.addEventListener("change",typeSelect.form.submit());
    	}
});

